# EDQSG 0.6.0 / U1.2 测试报告

## 测试结论

- 自动测试：**32项全部通过**；
- Python字节码编译：通过；
- SQL Server配置解析：三份AeroMRO关系控制配置全部通过；
- SQL Server假执行器全流程：通过；
- AeroMRO结构、场景、传播负载和真值目录检查：通过。

## U1.2重点覆盖

1. `RelationshipControlRule`配置解析与参数校验；
2. 预期关系与可信物理外键自动匹配；
3. 无物理外键、但应用实时校验和定期审计充分时，不自动降低SG3或触发关系底线；
4. 无物理外键且仅有文档约定时，识别为关系完整性控制不足并触发底线；
5. DQ6始终依据预期关系执行孤儿检测，不因物理外键删除而失去检测入口；
6. 基线、受控无外键、非受控无外键与控制缺口传播四组真值矩阵检查；
7. 旧版`logical_foreign_keys`配置兼容；
8. 单因素确定性敏感性和蒙特卡洛稳健性回归测试。

## 实际数据库验证边界

交付环境没有可用SQL Server实例，因此未执行真实数据库连接、DDL部署、执行计划和大规模负载测试。元数据采集、质量剖析和规则查询使用假执行器完成自动化验证。首次正式使用应在SQL Server 2019及以上测试实例或只读副本中完成兼容性、权限、超时和性能验证。
